name: Release Binaries

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Explain current release path
        run: |
          echo "Binary GitHub Releases are intentionally disabled for now."
          echo "The active release path is PyPI publishing through publish-pypi.yml."
          echo "Restore executable build artifacts before re-enabling binary releases."
