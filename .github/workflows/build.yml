name: Build Executables

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Explain current release path
        run: |
          echo "PyInstaller binary builds are intentionally disabled for now."
          echo "The active release path is PyPI publishing through publish-pypi.yml."
          echo "Restore build/voiceuse.spec before re-enabling executable builds."
